/*
 * Controller for showTeam.fxml.
 *
 * Referenced by showTeam.fxml since 2018 but never committed, so the
 * "Team Members" window has always failed to load. The FXML declares no
 * fx:id or onAction attributes, so nothing needs to be injected here.
 */
package esu.algorithm.UI;

import java.net.URL;
import java.util.ResourceBundle;
import javafx.fxml.Initializable;

/**
 * FXML Controller class
 *
 * @author dawitabera
 */
public class ShowTeamController implements Initializable {

    /**
     * Initializes the controller class.
     * @param url
     * @param rb
     */
    @Override
    public void initialize(URL url, ResourceBundle rb) {
        // The team screen is static; no wiring required.
    }

}
