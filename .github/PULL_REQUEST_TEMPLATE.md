## What this changes

<!-- What is different afterwards, and why. -->

## How you know it works

<!--
Tests are the usual answer. If you changed the algorithm, say what the oracle
test does with it. If you changed something visual, the view tests will say
whether it is still built and still readable, but only a screenshot says
whether it looks right.
-->

- [ ] `./gradlew test` passes
- [ ] `npm test` and `npm run test:browser` in `web/` pass, if you touched the
      browser version
- [ ] Changing one engine and not the other? The equivalence fixtures will
      say so — see `web/README.md`

## Anything left undone

<!-- Known gaps are easier to review than discovered ones. Delete if none. -->
